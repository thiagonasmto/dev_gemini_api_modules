package controllers

// import (
// 	"geminai_with_go/models"
// 	"net/http"

// 	"github.com/gin-gonic/gin"
// )

// func SaveEmbedding(c *gin.Context) {
// 	var embedding models.Document
// 	if err := c.ShouldBindJSON(&embedding); err != nil {
// 		c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})
// 		return
// 	}

// }
